import type { SessionResponse } from "../types";

// Default stub: in dynamic/server mode this is null. A compile-time codegen
// step can overwrite this file to embed a static SessionResponse payload.
export const staticSession: SessionResponse | null = null;
